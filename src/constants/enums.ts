export enum ConsoleType {
  Info = 'info',
  Error = 'error',
  Warn = 'warn',
}

export enum BookingChangeType {
  New = 'New',
  Cancelled = 'Cancelled',
  Extended = 'Extended',
  Shortened = 'Shortened',
}
